import { ShortLinkPipe } from './short-link.pipe';

describe('ShortLinkPipe', () => {
  it('create an instance', () => {
    const pipe = new ShortLinkPipe();
    expect(pipe).toBeTruthy();
  });
});
